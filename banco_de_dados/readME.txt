REALIZAR MERGE PARA DO BANCO DE DADOS

    git checkout master
    git pull origin master
    git merge database (utilize ":wq" para confirmar o merge)
    git push origin master


REALIZAR MERGE DO BACK-END

    git checkout master
    git pull origin master
    git merge backend (utilize ":wq" para confirmar o merge)
    git push origin master


REALIZAR MERGE DO FRONT-END

    git checkout master
    git pull origin master
    git merge frontend (utilize ":wq" para confirmar o merge)
    git push origin master


CLONAR REPOSITÓRIO

    git clone "https://github.com/YuriBrum/Sistema-ARYN.git"


NAVEGAÇÃO ENTRE BRANCH's

    git branch (visualizaão)
    git checkout "branch" (deslocamento/navegação)